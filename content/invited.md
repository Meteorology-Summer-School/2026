## 講演一覧

::invited::

## 更新方法

- 講演者名、所属、講演題目、講演要旨、学生へのメッセージは `data/invited_talks.csv` の各列を書き換えてください。
- 写真を使う場合は `assets/images/` に画像を置き、CSV の `image` 列のパスを変更してください。
- `anchor` 列はページ内リンク用です。通常はそのままで構いません。
